// usb_host_cdc_acm bindgen header — the IC-705's CI-V port over USB
// (m5stack-cores3-app `src/civ_usb.rs`). Its own `bindings_module`
// for the same reason as `bindings_uac.h`: keep each class driver's
// symbols in a module of their own.
#include "usb/cdc_acm_host.h"
